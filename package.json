{
  "name": "ie-ajv-tester",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@babel/standalone": "^7.0.0-beta.37",
    "ajv": "^6.0.1",
    "ajv-async": "^1.0.1",
    "babel-polyfill": "^6.26.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "ajv-compile": "ajv compile -s src/schema -o src/validateCompiled.js && ./postAjvCompile.js validateCompiled.js",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "ajv-cli": "^3.0.0"
  }
}
